<% if @categories.any? %>
  <% @categories.each do |category| %>
    <%= link_to category.title, admin_category_path(category) %>
    <% category.subcategories.each do |subcategory| %>
      <%= link_to subcategory.title, admin_categoty_path(subcategory)%>
    <% end %><br><br>
  <% end %>
<% end %>

<%= link_to "insert new category", new_admin_category_path %><br>
<%= link_to "insert new product",  new_admin_product_path %>
